const myFunc = (myName,age) => {
    console.log(myName,age)
  }

  const myFunc1 = myName => {
    console.log(myName)
  }
  let myName = "Ahrar1"
  myFunc(myName)

  const multiply = (number) => {
    console.log (number*2)
    return number*2
  }

  //same as above
  const multiply2 = (number) => number*2
  